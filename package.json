{
  "name": "wna-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",

  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:local": "CLIENT_ENV=local vite build",
    "build:dev": "CLIENT_ENV=development vite build",
    "build:prod": "CLIENT_ENV=production vite build",
    "build:main": "APP=main-app vite build",
    "build:main:local": "CLIENT_ENV=local APP=main-app vite build",
    "build:main:dev": "CLIENT_ENV=development APP=main-app vite build",
    "build:main:prod": "CLIENT_ENV=production APP=main-app vite build",
    "obfuscate": "vite build --mode production",
    "clean": "rimraf dist"
  },

  "dependencies": {
    "vue": "^3.5.24",
    "vue-router": "^4.6.3",
    "axios": "^1.13.2"
  },

  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "vite": "^7.2.4",
    "vite-plugin-vue-devtools": "^8.0.5",
    "rollup-plugin-obfuscator": "^1.1.0",
    "rimraf": "^6.1.2",
    "terser": "^5.44.1"
  }
}
